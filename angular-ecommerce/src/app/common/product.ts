export class Product {

    // constructor( sku:String, name:String, description:String, unitPrice:number,
    //     imageUrl:String, active:boolean, unitsInStock:number, dateCreated:Date, lastUpdated:Date  ){
    //     this.sku = sku, this.name = name, this.description = description, this.unitPrice = unitPrice,
    //     this.imageUrl = imageUrl, this.active = active, this.unitsInStock = unitsInStock,
    //     this.dateCreated = dateCreated, this.lastUpdated = lastUpdated
    // }

    sku: String;

    name: String;

    description: String;

    unitPrice: number;

    imageUrl: String;

    active: boolean;

    unitsInStock: number;

    dateCreated: Date;

    lastUpdated: Date;

}
